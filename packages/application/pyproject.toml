[project]
name = "cuantocuestave-application"
version = "0.1.0"
description = "Application layer: use cases, unit of work"
requires-python = ">=3.12"
dependencies = [
    "cuantocuestave-domain",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cuantocuestave_application"]
